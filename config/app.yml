# config/app.yml for rails-settings-cached
defaults: &defaults
  registry_endpoint: http://proxy
  self_registration: true
  allow_push: true

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
